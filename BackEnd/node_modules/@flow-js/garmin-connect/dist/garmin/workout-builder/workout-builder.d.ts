import { Step } from './step';
import { WorkoutType } from './workout-type';
import { IWorkoutDetail } from '../types';
export declare class WorkoutBuilder {
    private readonly workoutType;
    private workoutName;
    private workoutDescription;
    private steps;
    constructor(workoutType: WorkoutType, workoutName: string, workoutDescription?: string);
    addStep(step: Step): this;
    build(): IWorkoutDetail;
}
