export declare abstract class Target {
    abstract build(): any;
}
export declare class NoTarget extends Target {
    build(): any;
}
export declare class PaceTarget extends Target {
    private minPace;
    private maxPace;
    /**
     * Create a pace target
     * @param minPace - in seconds per meter
     * @param maxPace - in seconds per meter
     */
    constructor(minPace: number, maxPace: number);
    static pace(paceMinutes: number, paceSeconds: number, margin?: number): PaceTarget;
    build(): any;
}
export declare class CadenceTarget extends Target {
    private minCadence;
    private maxCadence;
    constructor(minCadence: number, maxCadence: number);
    static cadence(cadence: number, margin: number): CadenceTarget;
    build(): any;
}
export declare class HrmZoneTarget extends Target {
    private hrmZone;
    constructor(hrmZone: number);
    build(): any;
}
export declare class HrmTarget extends Target {
    private minHrm;
    private maxHrm;
    constructor(minHrm: number, maxHrm: number);
    static hrm(hrm: number, margin: number): HrmTarget;
    build(): any;
}
export declare class PowerZoneTarget extends Target {
    private powerZone;
    constructor(powerZone: number);
    build(): any;
}
export declare class PowerZone extends Target {
    private minPower;
    private maxPower;
    constructor(minPower: number, maxPower: number);
    static power(power: number, margin: number): PowerZone;
    build(): any;
}
